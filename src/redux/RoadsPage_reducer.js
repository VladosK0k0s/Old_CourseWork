let initialState = {
    kilometers: 1000000
}

const RoadsPage_reducer = (state = initialState, action) =>{
	switch (action.state) {
		default:
			return state;
	}
}

export default RoadsPage_reducer;